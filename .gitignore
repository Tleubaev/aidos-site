# Исходники от клиента (макеты упаковки, логотипы) — не деплоятся
/_content

# Окружение и зависимости
.env
.env.backup
/vendor
/node_modules

# Laravel (появится после согласования вёрстки)
/storage/*.key
/public/storage
/public/hot
/.phpunit.cache

# Системное
.DS_Store
Thumbs.db
*.log
